# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# Google Gemini API Configuration
# Get your API key from https://ai.google.dev/
GOOGLE_API_KEY=""

# API Base URL for Flutter app
# Replace YOUR_IP with your machine's IP address or domain
API_BASE_URL="http://YOUR_IP:8000"

# Database Configuration - Neon PostgreSQL
# Format: postgresql+asyncpg://user:password@host/dbname?ssl=require
# Note: Use ?ssl=require for asyncpg (FastAPI app). Alembic will convert to ?sslmode=require for psycopg2.
# Get your connection string from Neon Console: https://console.neon.tech
# Example: postgresql+asyncpg://user:password@ep-cool-name.neon.tech/neondb?ssl=require
DATABASE_URL="postgresql+asyncpg://user:password@your-neon-host/neondb?ssl=require"

# Server Configuration (optional - defaults to 0.0.0.0:8000)
HOST="0.0.0.0"
PORT="8000"

# CORS Origins (optional - defaults to "*" for development)
# For production, specify allowed origins: "http://localhost:3000,https://example.com"
CORS_ORIGINS="*"

# Encryption & Secret keys
# Generate with: python ./backend/scripts/generate_secrets.p
# JWT Authentication Configuration
# Secret key for signing JWT tokens (use a long random string in production)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY="your-secret-key-change-in-production"
JWT_ALGORITHM="HS256"
ENCRYPTION_KEY=""
# Access token expires in 7 days (10080 minutes) - kid-friendly, less frequent logins
ACCESS_TOKEN_EXPIRE_MINUTES="10080"
# Refresh token expires in 30 days
REFRESH_TOKEN_EXPIRE_DAYS="30"
